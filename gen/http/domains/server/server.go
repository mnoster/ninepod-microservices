// Code generated by goa v2.0.0-wip, DO NOT EDIT.
//
// domains HTTP server
//
// Command:
// $ goa gen ninepod/design

package server

import (
	"context"
	"net/http"
	domains "ninepod/gen/domains"

	goa "goa.design/goa"
	goahttp "goa.design/goa/http"
)

// Server lists the domains service endpoint HTTP handlers.
type Server struct {
	Mounts  []*MountPoint
	Domains http.Handler
}

// ErrorNamer is an interface implemented by generated error structs that
// exposes the name of the error as defined in the design.
type ErrorNamer interface {
	ErrorName() string
}

// MountPoint holds information about the mounted endpoints.
type MountPoint struct {
	// Method is the name of the service method served by the mounted HTTP handler.
	Method string
	// Verb is the HTTP method used to match requests to the mounted handler.
	Verb string
	// Pattern is the HTTP request path pattern used to match requests to the
	// mounted handler.
	Pattern string
}

// New instantiates HTTP handlers for all the domains service endpoints.
func New(
	e *domains.Endpoints,
	mux goahttp.Muxer,
	dec func(*http.Request) goahttp.Decoder,
	enc func(context.Context, http.ResponseWriter) goahttp.Encoder,
	eh func(context.Context, http.ResponseWriter, error),
) *Server {
	return &Server{
		Mounts: []*MountPoint{
			{"Domains", "GET", "/domains/{a}/{b}"},
		},
		Domains: NewDomainsHandler(e.Domains, mux, dec, enc, eh),
	}
}

// Service returns the name of the service served.
func (s *Server) Service() string { return "domains" }

// Use wraps the server handlers with the given middleware.
func (s *Server) Use(m func(http.Handler) http.Handler) {
	s.Domains = m(s.Domains)
}

// Mount configures the mux to serve the domains endpoints.
func Mount(mux goahttp.Muxer, h *Server) {
	MountDomainsHandler(mux, h.Domains)
}

// MountDomainsHandler configures the mux to serve the "domains" service
// "domains" endpoint.
func MountDomainsHandler(mux goahttp.Muxer, h http.Handler) {
	f, ok := h.(http.HandlerFunc)
	if !ok {
		f = func(w http.ResponseWriter, r *http.Request) {
			h.ServeHTTP(w, r)
		}
	}
	mux.Handle("GET", "/domains/{a}/{b}", f)
}

// NewDomainsHandler creates a HTTP handler which loads the HTTP request and
// calls the "domains" service "domains" endpoint.
func NewDomainsHandler(
	endpoint goa.Endpoint,
	mux goahttp.Muxer,
	dec func(*http.Request) goahttp.Decoder,
	enc func(context.Context, http.ResponseWriter) goahttp.Encoder,
	eh func(context.Context, http.ResponseWriter, error),
) http.Handler {
	var (
		decodeRequest  = DecodeDomainsRequest(mux, dec)
		encodeResponse = EncodeDomainsResponse(enc)
		encodeError    = goahttp.ErrorEncoder(enc)
	)
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := context.WithValue(r.Context(), goahttp.AcceptTypeKey, r.Header.Get("Accept"))
		ctx = context.WithValue(ctx, goa.MethodKey, "domains")
		ctx = context.WithValue(ctx, goa.ServiceKey, "domains")
		payload, err := decodeRequest(r)
		if err != nil {
			if err := encodeError(ctx, w, err); err != nil {
				eh(ctx, w, err)
			}
			return
		}

		res, err := endpoint(ctx, payload)

		if err != nil {
			if err := encodeError(ctx, w, err); err != nil {
				eh(ctx, w, err)
			}
			return
		}
		if err := encodeResponse(ctx, w, res); err != nil {
			eh(ctx, w, err)
		}
	})
}
