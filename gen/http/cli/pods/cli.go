// Code generated by goa v2.0.0-wip, DO NOT EDIT.
//
// pods HTTP client CLI support package
//
// Command:
// $ goa gen ninepod/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	domainsc "ninepod/gen/http/domains/client"
	"os"

	goa "goa.design/goa"
	goahttp "goa.design/goa/http"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `domains domains
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` domains domains --a 7674819484560861460 --b "Fugiat saepe impedit non assumenda quia iste."` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		domainsFlags = flag.NewFlagSet("domains", flag.ContinueOnError)

		domainsDomainsFlags = flag.NewFlagSet("domains", flag.ExitOnError)
		domainsDomainsAFlag = domainsDomainsFlags.String("a", "REQUIRED", "number")
		domainsDomainsBFlag = domainsDomainsFlags.String("b", "REQUIRED", "category")
	)
	domainsFlags.Usage = domainsUsage
	domainsDomainsFlags.Usage = domainsDomainsUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if len(os.Args) < flag.NFlag()+3 {
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = os.Args[1+flag.NFlag()]
		switch svcn {
		case "domains":
			svcf = domainsFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(os.Args[2+flag.NFlag():]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = os.Args[2+flag.NFlag()+svcf.NFlag()]
		switch svcn {
		case "domains":
			switch epn {
			case "domains":
				epf = domainsDomainsFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if len(os.Args) > 2+flag.NFlag()+svcf.NFlag() {
		if err := epf.Parse(os.Args[3+flag.NFlag()+svcf.NFlag():]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "domains":
			c := domainsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "domains":
				endpoint = c.Domains()
				data, err = domainsc.BuildDomainsPayload(*domainsDomainsAFlag, *domainsDomainsBFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// domainsUsage displays the usage of the domains command and its subcommands.
func domainsUsage() {
	fmt.Fprintf(os.Stderr, `Get pods, categories, etc.
Usage:
    %s [globalflags] domains COMMAND [flags]

COMMAND:
    domains: Domains implements domains.

Additional help:
    %s domains COMMAND --help
`, os.Args[0], os.Args[0])
}
func domainsDomainsUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] domains domains -a INT -b STRING

Domains implements domains.
    -a INT: number
    -b STRING: category

Example:
    `+os.Args[0]+` domains domains --a 7674819484560861460 --b "Fugiat saepe impedit non assumenda quia iste."
`, os.Args[0])
}
